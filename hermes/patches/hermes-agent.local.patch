diff --git a/agent/model_metadata.py b/agent/model_metadata.py
index 3b500664..1c5b6fca 100644
--- a/agent/model_metadata.py
+++ b/agent/model_metadata.py
@@ -218,6 +218,7 @@ _URL_TO_PROVIDER: Dict[str, str] = {
     "api.openai.com": "openai",
     "chatgpt.com": "openai",
     "api.anthropic.com": "anthropic",
+    "gaccode.com": "anthropic",
     "api.z.ai": "zai",
     "api.moonshot.ai": "kimi-coding",
     "api.moonshot.cn": "kimi-coding-cn",
@@ -1001,6 +1002,7 @@ def get_model_context_length(
                 "in config.yaml to override.",
                 model, base_url, f"{DEFAULT_FALLBACK_CONTEXT:,}",
             )
+            save_context_length(model, base_url, DEFAULT_FALLBACK_CONTEXT)
             return DEFAULT_FALLBACK_CONTEXT
 
     # 4. Anthropic /v1/models API (only for regular API keys, not OAuth)
diff --git a/agent/prompt_builder.py b/agent/prompt_builder.py
index c61d6995..6078b385 100644
--- a/agent/prompt_builder.py
+++ b/agent/prompt_builder.py
@@ -364,6 +364,11 @@ PLATFORM_HINTS = {
         "documents. You can also include image URLs in markdown format ![alt](url) and they "
         "will be downloaded and sent as native media when possible."
     ),
+    "daxiang": (
+        "You are on DaXiang (美团大象). Keep messages concise and readable for enterprise chat. "
+        "Prefer plain text or light markdown that stays easy to scan on mobile. In group chats, "
+        "be extra concise and action-oriented."
+    ),
     "wecom": (
         "You are on WeCom (企业微信 / Enterprise WeChat). Markdown formatting is supported. "
         "You CAN send media files natively — to deliver a file to the user, include "
diff --git a/cron/scheduler.py b/cron/scheduler.py
index 83b7abb9..3d1df365 100644
--- a/cron/scheduler.py
+++ b/cron/scheduler.py
@@ -44,7 +44,7 @@ logger = logging.getLogger(__name__)
 _KNOWN_DELIVERY_PLATFORMS = frozenset({
     "telegram", "discord", "slack", "whatsapp", "signal",
     "matrix", "mattermost", "homeassistant", "dingtalk", "feishu",
-    "wecom", "wecom_callback", "weixin", "sms", "email", "webhook", "bluebubbles",
+    "wecom", "wecom_callback", "weixin", "daxiang", "sms", "email", "webhook", "bluebubbles",
     "qqbot",
 })
 
@@ -244,6 +244,7 @@ def _deliver_result(job: dict, content: str, adapters=None, loop=None) -> Option
         "slack": Platform.SLACK,
         "whatsapp": Platform.WHATSAPP,
         "signal": Platform.SIGNAL,
+        "daxiang": Platform.DAXIANG,
         "matrix": Platform.MATRIX,
         "mattermost": Platform.MATTERMOST,
         "homeassistant": Platform.HOMEASSISTANT,
diff --git a/gateway/config.py b/gateway/config.py
index 7ce105f3..66b4ca21 100644
--- a/gateway/config.py
+++ b/gateway/config.py
@@ -65,6 +65,7 @@ class Platform(Enum):
     WECOM = "wecom"
     WECOM_CALLBACK = "wecom_callback"
     WEIXIN = "weixin"
+    DAXIANG = "daxiang"
     BLUEBUBBLES = "bluebubbles"
     QQBOT = "qqbot"
 
@@ -194,7 +195,7 @@ class StreamingConfig:
     transport: str = "edit"       # "edit" (progressive editMessageText) or "off"
     edit_interval: float = 1.0    # Seconds between message edits (Telegram rate-limits at ~1/s)
     buffer_threshold: int = 40    # Chars before forcing an edit
-    cursor: str = " ▉"           # Cursor shown during streaming
+    cursor: str = ""             # Cursor shown during streaming
 
     def to_dict(self) -> Dict[str, Any]:
         return {
@@ -214,7 +215,7 @@ class StreamingConfig:
             transport=data.get("transport", "edit"),
             edit_interval=float(data.get("edit_interval", 1.0)),
             buffer_threshold=int(data.get("buffer_threshold", 40)),
-            cursor=data.get("cursor", " ▉"),
+            cursor=data.get("cursor", ""),
         )
 
 
@@ -269,6 +270,11 @@ class GatewayConfig:
                 if config.extra.get("account_id") and (config.token or config.extra.get("token")):
                     connected.append(platform)
                 continue
+            if platform == Platform.DAXIANG:
+                token_path = str(config.extra.get("token_storage_path") or "").strip()
+                if config.token or token_path:
+                    connected.append(platform)
+                continue
             # Platforms that use token/api_key auth
             if config.token or config.api_key:
                 connected.append(platform)
@@ -709,6 +715,7 @@ def _validate_gateway_config(config: "GatewayConfig") -> None:
         Platform.MATTERMOST: "MATTERMOST_TOKEN",
         Platform.MATRIX: "MATRIX_ACCESS_TOKEN",
         Platform.WEIXIN: "WEIXIN_TOKEN",
+        Platform.DAXIANG: "DAXIANG_ACCESS_TOKEN",
     }
     for platform, pconfig in config.platforms.items():
         if not pconfig.enabled:
@@ -1095,6 +1102,53 @@ def _apply_env_overrides(config: GatewayConfig) -> None:
                 name=os.getenv("WEIXIN_HOME_CHANNEL_NAME", "Home"),
             )
 
+    daxiang_enabled = os.getenv("DAXIANG_ENABLED", "").strip().lower() in ("true", "1", "yes", "on")
+    daxiang_access_token = os.getenv("DAXIANG_ACCESS_TOKEN", "").strip()
+    daxiang_token_path = os.getenv("DAXIANG_TOKEN_STORAGE_PATH", "").strip()
+    daxiang_user_mis = os.getenv("DAXIANG_USER_MIS", "").strip()
+    daxiang_bot_id = os.getenv("DAXIANG_BOT_ID", "").strip()
+    daxiang_base_url = os.getenv("DAXIANG_BASE_URL", "").strip()
+    if daxiang_enabled or daxiang_access_token or daxiang_token_path or daxiang_user_mis:
+        if Platform.DAXIANG not in config.platforms:
+            config.platforms[Platform.DAXIANG] = PlatformConfig()
+        config.platforms[Platform.DAXIANG].enabled = True
+        if daxiang_access_token:
+            config.platforms[Platform.DAXIANG].token = daxiang_access_token
+        extra = config.platforms[Platform.DAXIANG].extra
+        if daxiang_token_path:
+            extra["token_storage_path"] = daxiang_token_path
+        if daxiang_user_mis:
+            extra["user_mis"] = daxiang_user_mis
+        if daxiang_bot_id:
+            extra["bot_id"] = daxiang_bot_id
+        if daxiang_base_url:
+            extra["base_url"] = daxiang_base_url.rstrip("/")
+        daxiang_poll_ms = os.getenv("DAXIANG_POLL_INTERVAL_MS", "").strip()
+        if daxiang_poll_ms:
+            try:
+                extra["poll_interval_ms"] = int(daxiang_poll_ms)
+            except ValueError:
+                pass
+        daxiang_batch_size = os.getenv("DAXIANG_BATCH_SIZE", "").strip()
+        if daxiang_batch_size:
+            try:
+                extra["batch_size"] = int(daxiang_batch_size)
+            except ValueError:
+                pass
+        daxiang_allow_from = os.getenv("DAXIANG_ALLOWED_USERS", "").strip()
+        if daxiang_allow_from:
+            extra["allow_from"] = daxiang_allow_from
+        daxiang_group_allow_from = os.getenv("DAXIANG_GROUP_ALLOWED_USERS", "").strip()
+        if daxiang_group_allow_from:
+            extra["group_allow_from"] = daxiang_group_allow_from
+        daxiang_home = os.getenv("DAXIANG_HOME_CHANNEL", "").strip()
+        if daxiang_home:
+            config.platforms[Platform.DAXIANG].home_channel = HomeChannel(
+                platform=Platform.DAXIANG,
+                chat_id=daxiang_home,
+                name=os.getenv("DAXIANG_HOME_CHANNEL_NAME", "Home"),
+            )
+
     # BlueBubbles (iMessage)
     bluebubbles_server_url = os.getenv("BLUEBUBBLES_SERVER_URL")
     bluebubbles_password = os.getenv("BLUEBUBBLES_PASSWORD")
diff --git a/gateway/platforms/base.py b/gateway/platforms/base.py
index f7943da4..682bdd51 100644
--- a/gateway/platforms/base.py
+++ b/gateway/platforms/base.py
@@ -20,6 +20,15 @@ from urllib.parse import urlsplit
 
 logger = logging.getLogger(__name__)
 
+_TRAILING_DISPLAY_BLOCK_RE = re.compile(r'(?:\s*[▉█■□▪◼◾⬛⬜]+\s*)+$')
+
+
+def _strip_trailing_display_blocks(text: str) -> str:
+    """Remove stuck trailing block/cursor glyphs from final user-visible text."""
+    if not text:
+        return text
+    return _TRAILING_DISPLAY_BLOCK_RE.sub("", text).rstrip()
+
 
 def utf16_len(s: str) -> int:
     """Count UTF-16 code units in *s*.
@@ -1518,6 +1527,7 @@ class BasePlatformAdapter(ABC):
                     _thread_meta = {"thread_id": event.source.thread_id} if event.source.thread_id else None
                     response = await self._message_handler(event)
                     if response:
+                        response = _strip_trailing_display_blocks(response)
                         await self._send_with_retry(
                             chat_id=event.source.chat_id,
                             content=response,
@@ -1617,8 +1627,25 @@ class BasePlatformAdapter(ABC):
         try:
             await self._run_processing_hook("on_processing_start", event)
 
-            # Call the handler (this can take a while with tool calls)
-            response = await self._message_handler(event)
+            # Call the handler (this can take a while with tool calls).
+            # Some platform/provider combinations can hang silently and leave
+            # the user with no reply at all. Put a bounded timeout around the
+            # end-to-end handler so we fail closed with a user-visible message
+            # instead of radio silence.
+            _handler_timeout = 0.0
+            try:
+                _timeout_env = os.getenv("HERMES_MESSAGE_HANDLER_TIMEOUT", "").strip()
+                if _timeout_env:
+                    _handler_timeout = max(float(_timeout_env), 0.0)
+                elif getattr(self.platform, "value", "") == "daxiang":
+                    _handler_timeout = 120.0
+            except Exception:
+                _handler_timeout = 0.0
+
+            if _handler_timeout > 0:
+                response = await asyncio.wait_for(self._message_handler(event), timeout=_handler_timeout)
+            else:
+                response = await self._message_handler(event)
             
             # Send response if any.  A None/empty response is normal when
             # streaming already delivered the text (already_sent=True) or
@@ -1635,6 +1662,7 @@ class BasePlatformAdapter(ABC):
                 # Strip any remaining internal directives from message body (fixes #1561)
                 text_content = text_content.replace("[[audio_as_voice]]", "").strip()
                 text_content = re.sub(r"MEDIA:\s*\S+", "", text_content).strip()
+                text_content = _strip_trailing_display_blocks(text_content)
                 if images:
                     logger.info("[%s] extract_images found %d image(s) in response (%d chars)", self.name, len(images), len(response))
 
@@ -1833,17 +1861,23 @@ class BasePlatformAdapter(ABC):
                 error_type = type(e).__name__
                 error_detail = str(e)[:300] if str(e) else "no details available"
                 _thread_metadata = {"thread_id": event.source.thread_id} if event.source.thread_id else None
-                await self.send(
-                    chat_id=event.source.chat_id,
-                    content=(
-                        f"Sorry, I encountered an error ({error_type}).\n"
-                        f"{error_detail}\n"
-                        "Try again or use /reset to start a fresh session."
-                    ),
-                    metadata=_thread_metadata,
+                _error_message = (
+                    f"Sorry, I encountered an error ({error_type}).\n"
+                    f"{error_detail}\n"
+                    "Try again or use /reset to start a fresh session."
+                )
+                logger.info("[%s] Sending fallback error message to %s", self.name, event.source.chat_id)
+                _send_result = await asyncio.shield(
+                    self.send(
+                        chat_id=event.source.chat_id,
+                        content=_error_message,
+                        metadata=_thread_metadata,
+                    )
                 )
+                if _send_result and not _send_result.success:
+                    logger.error("[%s] Fallback error message send failed: %s", self.name, _send_result.error)
             except Exception:
-                pass  # Last resort — don't let error reporting crash the handler
+                logger.exception("[%s] Failed to deliver fallback error message", self.name)
         finally:
             # Stop typing indicator
             typing_task.cancel()
diff --git a/gateway/run.py b/gateway/run.py
index c8c25256..81000462 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -1498,6 +1498,7 @@ class GatewayRunner:
                        "WECOM_ALLOWED_USERS",
                        "WECOM_CALLBACK_ALLOWED_USERS",
                        "WEIXIN_ALLOWED_USERS",
+                       "DAXIANG_ALLOWED_USERS",
                        "BLUEBUBBLES_ALLOWED_USERS",
                        "QQ_ALLOWED_USERS",
                        "GATEWAY_ALLOWED_USERS")
@@ -1513,6 +1514,7 @@ class GatewayRunner:
                        "WECOM_ALLOW_ALL_USERS",
                        "WECOM_CALLBACK_ALLOW_ALL_USERS",
                        "WEIXIN_ALLOW_ALL_USERS",
+                       "DAXIANG_ALLOW_ALL_USERS",
                        "BLUEBUBBLES_ALLOW_ALL_USERS",
                        "QQ_ALLOW_ALL_USERS")
         )
@@ -2220,6 +2222,13 @@ class GatewayRunner:
                 return None
             return WeixinAdapter(config)
 
+        elif platform == Platform.DAXIANG:
+            from gateway.platforms.daxiang import DaxiangAdapter, check_daxiang_requirements
+            if not check_daxiang_requirements():
+                logger.warning("DaXiang: httpx not installed")
+                return None
+            return DaxiangAdapter(config)
+
         elif platform == Platform.MATTERMOST:
             from gateway.platforms.mattermost import MattermostAdapter, check_mattermost_requirements
             if not check_mattermost_requirements():
@@ -2304,6 +2313,7 @@ class GatewayRunner:
             Platform.WECOM: "WECOM_ALLOWED_USERS",
             Platform.WECOM_CALLBACK: "WECOM_CALLBACK_ALLOWED_USERS",
             Platform.WEIXIN: "WEIXIN_ALLOWED_USERS",
+            Platform.DAXIANG: "DAXIANG_ALLOWED_USERS",
             Platform.BLUEBUBBLES: "BLUEBUBBLES_ALLOWED_USERS",
             Platform.QQBOT: "QQ_ALLOWED_USERS",
         }
@@ -2322,6 +2332,7 @@ class GatewayRunner:
             Platform.WECOM: "WECOM_ALLOW_ALL_USERS",
             Platform.WECOM_CALLBACK: "WECOM_CALLBACK_ALLOW_ALL_USERS",
             Platform.WEIXIN: "WEIXIN_ALLOW_ALL_USERS",
+            Platform.DAXIANG: "DAXIANG_ALLOW_ALL_USERS",
             Platform.BLUEBUBBLES: "BLUEBUBBLES_ALLOW_ALL_USERS",
             Platform.QQBOT: "QQ_ALLOW_ALL_USERS",
         }
@@ -7444,6 +7455,15 @@ class GatewayRunner:
         # display.<key> global, then built-in platform defaults.
         from gateway.display_config import resolve_display_setting
 
+        context_pressure_notifications_enabled = bool(
+            resolve_display_setting(
+                user_config,
+                platform_key,
+                "context_pressure_notifications",
+                False,
+            )
+        )
+
         # Apply tool preview length config (0 = no limit)
         try:
             from agent.display import set_tool_preview_max_len
@@ -7723,6 +7743,8 @@ class GatewayRunner:
         def _status_callback_sync(event_type: str, message: str) -> None:
             if not _status_adapter:
                 return
+            if event_type == "context_pressure" and not context_pressure_notifications_enabled:
+                return
             try:
                 asyncio.run_coroutine_threadsafe(
                     _status_adapter.send(
@@ -7873,6 +7895,25 @@ class GatewayRunner:
 
             turn_route = self._resolve_turn_agent_config(message, model, runtime_kwargs)
 
+            runtime_note_parts = [
+                "[System note: Current live runtime for this turn",
+                f"model={turn_route.get('model') or model}",
+            ]
+            runtime = turn_route.get("runtime") or {}
+            runtime_provider = runtime.get("provider") or runtime_kwargs.get("provider")
+            runtime_base_url = runtime.get("base_url") or runtime_kwargs.get("base_url")
+            if runtime_provider:
+                runtime_note_parts.append(f"provider={runtime_provider}")
+            if runtime_base_url:
+                runtime_note_parts.append(f"base_url={runtime_base_url}")
+            runtime_note = "; ".join(runtime_note_parts) + ". If the user asks which model/provider is being used right now, answer from this note rather than older conversation history or summaries.]"
+
+            combined_ephemeral_with_runtime = (
+                (combined_ephemeral + "\n\n" + runtime_note).strip()
+                if combined_ephemeral
+                else runtime_note
+            )
+
             # Check agent cache — reuse the AIAgent from the previous message
             # in this session to preserve the frozen system prompt and tool
             # schemas for prompt cache hits.
@@ -7880,7 +7921,7 @@ class GatewayRunner:
                 turn_route["model"],
                 turn_route["runtime"],
                 enabled_toolsets,
-                combined_ephemeral,
+                combined_ephemeral_with_runtime,
             )
             agent = None
             _cache_lock = getattr(self, "_agent_cache_lock", None)
@@ -7901,7 +7942,7 @@ class GatewayRunner:
                     quiet_mode=True,
                     verbose_logging=False,
                     enabled_toolsets=enabled_toolsets,
-                    ephemeral_system_prompt=combined_ephemeral or None,
+                    ephemeral_system_prompt=combined_ephemeral_with_runtime or None,
                     prefill_messages=self._prefill_messages or None,
                     reasoning_config=reasoning_config,
                     service_tier=self._service_tier,
@@ -8373,6 +8414,12 @@ class GatewayRunner:
             _agent_warning_raw = float(os.getenv("HERMES_AGENT_TIMEOUT_WARNING", 900))
             _agent_warning = _agent_warning_raw if _agent_warning_raw > 0 else None
             _warning_fired = False
+            _agent_for_turn = agent_holder[0]
+            if _agent_for_turn and hasattr(_agent_for_turn, "reset_activity"):
+                try:
+                    _agent_for_turn.reset_activity("starting new gateway request")
+                except Exception:
+                    logger.debug("Failed to reset agent activity before gateway run", exc_info=True)
             loop = asyncio.get_event_loop()
             _executor_task = asyncio.ensure_future(
                 loop.run_in_executor(None, run_sync)
@@ -8442,10 +8489,9 @@ class GatewayRunner:
                             try:
                                 await _warn_adapter.send(
                                     source.chat_id,
-                                    f"⚠️ No activity for {_elapsed_warn} min. "
-                                    f"If the agent does not respond soon, it will "
-                                    f"be timed out in {_remaining_mins} min. "
-                                    f"You can continue waiting or use /reset.",
+                                    f"【状态提示】本轮已 {_elapsed_warn} 分钟无新进展；"
+                                    f"若继续无响应，约 {_remaining_mins} 分钟后将自动结束。"
+                                    f"你可以继续等待，或用 /reset 重新开始。",
                                     metadata=_status_thread_metadata,
                                 )
                             except Exception as _warn_err:
@@ -8504,25 +8550,19 @@ class GatewayRunner:
 
                 # Construct a user-facing message with diagnostic context.
                 _diag_lines = [
-                    f"⏱️ Agent inactive for {_timeout_mins} min — no tool calls "
-                    f"or API responses."
+                    f"【状态提示】本轮已自动结束：{_timeout_mins} 分钟内没有新的工具调用或模型响应。"
                 ]
                 if _cur_tool:
                     _diag_lines.append(
-                        f"The agent appears stuck on tool `{_cur_tool}` "
-                        f"({_secs_ago:.0f}s since last activity, "
-                        f"iteration {_iter_n}/{_iter_max})."
+                        f"最后卡在工具 `{_cur_tool}`（距上次活动 {_secs_ago:.0f} 秒，"
+                        f"第 {_iter_n}/{_iter_max} 轮）。"
                     )
                 else:
                     _diag_lines.append(
-                        f"Last activity: {_last_desc} ({_secs_ago:.0f}s ago, "
-                        f"iteration {_iter_n}/{_iter_max}). "
-                        "The agent may have been waiting on an API response."
+                        f"最后活动：{_last_desc}（{_secs_ago:.0f} 秒前，第 {_iter_n}/{_iter_max} 轮）。"
                     )
                 _diag_lines.append(
-                    "To increase the limit, set agent.gateway_timeout in config.yaml "
-                    "(value in seconds, 0 = no limit) and restart the gateway.\n"
-                    "Try again, or use /reset to start fresh."
+                    "可直接继续发新消息；如果想清空上下文，再用 /reset。"
                 )
 
                 response = {
diff --git a/gateway/stream_consumer.py b/gateway/stream_consumer.py
index e6d96c80..ca0169d7 100644
--- a/gateway/stream_consumer.py
+++ b/gateway/stream_consumer.py
@@ -42,7 +42,7 @@ class StreamConsumerConfig:
     """Runtime config for a single stream consumer instance."""
     edit_interval: float = 1.0
     buffer_threshold: int = 40
-    cursor: str = " ▉"
+    cursor: str = ""
 
 
 class GatewayStreamConsumer:
@@ -435,8 +435,6 @@ class GatewayStreamConsumer:
         stream finishes — we just need to hide the raw directives from the
         user.
         """
-        if "MEDIA:" not in text and "[[audio_as_voice]]" not in text:
-            return text
         cleaned = text.replace("[[audio_as_voice]]", "")
         cleaned = GatewayStreamConsumer._MEDIA_RE.sub("", cleaned)
         # Collapse excessive blank lines left behind by removed tags
diff --git a/hermes_cli/config.py b/hermes_cli/config.py
index 78cc3015..ff089533 100644
--- a/hermes_cli/config.py
+++ b/hermes_cli/config.py
@@ -507,6 +507,7 @@ DEFAULT_CONFIG = {
         "tool_progress_command": False,  # Enable /verbose command in messaging gateway
         "tool_progress_overrides": {},  # DEPRECATED — use display.platforms instead
         "tool_preview_length": 0,  # Max chars for tool call previews (0 = no limit, show full paths/commands)
+        "context_pressure_notifications": False,  # Gateway: suppress "X% to compaction" chat notices by default
         "platforms": {},  # Per-platform display overrides: {"telegram": {"tool_progress": "all"}, "slack": {"tool_progress": "off"}}
     },
 
diff --git a/hermes_cli/platforms.py b/hermes_cli/platforms.py
index 1fc3a3a8..27cb4dd9 100644
--- a/hermes_cli/platforms.py
+++ b/hermes_cli/platforms.py
@@ -35,6 +35,7 @@ PLATFORMS: OrderedDict[str, PlatformInfo] = OrderedDict([
     ("wecom",          PlatformInfo(label="💬 WeCom",           default_toolset="hermes-wecom")),
     ("wecom_callback", PlatformInfo(label="💬 WeCom Callback",  default_toolset="hermes-wecom-callback")),
     ("weixin",         PlatformInfo(label="💬 Weixin",          default_toolset="hermes-weixin")),
+    ("daxiang",        PlatformInfo(label="🦞 DaXiang",         default_toolset="hermes-daxiang")),
     ("qqbot",          PlatformInfo(label="💬 QQBot",           default_toolset="hermes-qqbot")),
     ("webhook",        PlatformInfo(label="🔗 Webhook",         default_toolset="hermes-webhook")),
     ("api_server",     PlatformInfo(label="🌐 API Server",      default_toolset="hermes-api-server")),
diff --git a/run_agent.py b/run_agent.py
index 626951b2..2e66da02 100644
--- a/run_agent.py
+++ b/run_agent.py
@@ -2946,6 +2946,11 @@ class AIAgent:
         self._last_activity_ts = time.time()
         self._last_activity_desc = desc
 
+    def reset_activity(self, desc: str = "starting new request") -> None:
+        """Reset per-request activity tracking before a fresh gateway/CLI turn."""
+        self._current_tool = None
+        self._touch_activity(desc)
+
     def _capture_rate_limits(self, http_response: Any) -> None:
         """Parse x-ratelimit-* headers from an HTTP response and cache the state.
 
diff --git a/tests/gateway/test_gateway_inactivity_timeout.py b/tests/gateway/test_gateway_inactivity_timeout.py
index 598f3381..2d3fd7c1 100644
--- a/tests/gateway/test_gateway_inactivity_timeout.py
+++ b/tests/gateway/test_gateway_inactivity_timeout.py
@@ -48,6 +48,11 @@ class FakeAgent:
         self._interrupted = True
         self._interrupt_msg = msg
 
+    def reset_activity(self, desc="starting new gateway request"):
+        self._idle_seconds = 0.0
+        self._activity_desc = desc
+        self._current_tool = None
+
     def run_conversation(self, prompt):
         return {"final_response": "Done", "messages": []}
 
@@ -238,6 +243,22 @@ class TestStagedInactivityWarning:
         assert _warning_fired
         assert _inactivity_timeout
 
+    def test_reset_activity_prevents_immediate_repeat_timeout(self):
+        """A fresh turn should not inherit the previous turn's stale idle timer."""
+        agent = FakeAgent(
+            idle_seconds=1900.0,
+            activity_desc="API call #2 completed",
+            api_call_count=2,
+            max_iterations=90,
+        )
+
+        agent.reset_activity("starting new gateway request")
+        summary = agent.get_activity_summary()
+
+        assert summary["seconds_since_activity"] == 0.0
+        assert summary["last_activity_desc"] == "starting new gateway request"
+        assert summary["current_tool"] is None
+
     def test_warning_env_var_respected(self, monkeypatch):
         """HERMES_AGENT_TIMEOUT_WARNING env var is parsed correctly."""
         monkeypatch.setenv("HERMES_AGENT_TIMEOUT_WARNING", "600")
diff --git a/tests/gateway/test_run_progress_topics.py b/tests/gateway/test_run_progress_topics.py
index 7859edd7..8c53853f 100644
--- a/tests/gateway/test_run_progress_topics.py
+++ b/tests/gateway/test_run_progress_topics.py
@@ -54,6 +54,10 @@ class ProgressCaptureAdapter(BasePlatformAdapter):
         return {"id": chat_id}
 
 
+class NonEditableProgressAdapter(ProgressCaptureAdapter):
+    SUPPORTS_MESSAGE_EDITING = False
+
+
 class FakeAgent:
     def __init__(self, **kwargs):
         self.tool_progress_callback = kwargs.get("tool_progress_callback")
@@ -448,6 +452,7 @@ async def _run_with_agent(
     chat_id="-1001",
     chat_type="group",
     thread_id="17585",
+    adapter_cls=ProgressCaptureAdapter,
 ):
     if config_data:
         import yaml
@@ -462,7 +467,7 @@ async def _run_with_agent(
     fake_run_agent.AIAgent = agent_cls
     monkeypatch.setitem(sys.modules, "run_agent", fake_run_agent)
 
-    adapter = ProgressCaptureAdapter(platform=platform)
+    adapter = adapter_cls(platform=platform)
     runner = _make_runner(adapter)
     gateway_run = importlib.import_module("gateway.run")
     if config_data and "streaming" in config_data:
@@ -497,6 +502,21 @@ async def _run_with_agent(
     return adapter, result
 
 
+class RuntimeNoteCaptureAgent:
+    last_ephemeral_prompt = None
+
+    def __init__(self, **kwargs):
+        type(self).last_ephemeral_prompt = kwargs.get("ephemeral_system_prompt")
+        self.tools = []
+
+    def run_conversation(self, message, conversation_history=None, task_id=None):
+        return {
+            "final_response": "done",
+            "messages": [],
+            "api_calls": 1,
+        }
+
+
 @pytest.mark.asyncio
 async def test_run_agent_surfaces_real_interim_commentary(monkeypatch, tmp_path):
     adapter, result = await _run_with_agent(
@@ -572,6 +592,83 @@ async def test_run_agent_streaming_does_not_enable_completed_interim_commentary(
     assert not any(call["content"] == "I'll inspect the repo first." for call in adapter.sent)
 
 
+@pytest.mark.asyncio
+async def test_run_agent_noneditable_platform_skips_streaming_preview(monkeypatch, tmp_path):
+    adapter, result = await _run_with_agent(
+        monkeypatch,
+        tmp_path,
+        StreamingRefineAgent,
+        session_id="sess-noneditable-streaming",
+        config_data={
+            "display": {"tool_progress": "off", "interim_assistant_messages": False},
+            "streaming": {"enabled": True, "edit_interval": 0.01, "buffer_threshold": 1},
+        },
+        platform=Platform.DAXIANG,
+        chat_id="mis:test-user",
+        chat_type="dm",
+        thread_id=None,
+        adapter_cls=NonEditableProgressAdapter,
+    )
+
+    assert result.get("already_sent") is not True
+    assert adapter.sent == []
+
+
+@pytest.mark.asyncio
+async def test_run_agent_injects_live_runtime_note(monkeypatch, tmp_path):
+    RuntimeNoteCaptureAgent.last_ephemeral_prompt = None
+
+    fake_dotenv = types.ModuleType("dotenv")
+    fake_dotenv.load_dotenv = lambda *args, **kwargs: None
+    monkeypatch.setitem(sys.modules, "dotenv", fake_dotenv)
+
+    fake_run_agent = types.ModuleType("run_agent")
+    fake_run_agent.AIAgent = RuntimeNoteCaptureAgent
+    monkeypatch.setitem(sys.modules, "run_agent", fake_run_agent)
+
+    adapter = ProgressCaptureAdapter(platform=Platform.DAXIANG)
+    runner = _make_runner(adapter)
+    gateway_run = importlib.import_module("gateway.run")
+    monkeypatch.setattr(gateway_run, "_hermes_home", tmp_path)
+    monkeypatch.setattr(
+        gateway_run,
+        "_resolve_runtime_agent_kwargs",
+        lambda: {
+            "api_key": "***",
+            "provider": "gac_anthropic",
+            "base_url": "https://gaccode.com/claudecode",
+        },
+    )
+    runner._resolve_turn_agent_config = lambda message, model, runtime: {
+        "model": "claude-sonnet-4-6",
+        "runtime": {
+            "provider": "gac_anthropic",
+            "base_url": "https://gaccode.com/claudecode",
+        },
+    }
+
+    source = SessionSource(
+        platform=Platform.DAXIANG,
+        chat_id="mis:test-user",
+        chat_type="dm",
+        thread_id=None,
+    )
+
+    await runner._run_agent(
+        message="你现在模型是什么？",
+        context_prompt="",
+        history=[],
+        source=source,
+        session_id="sess-runtime-note",
+        session_key="agent:main:daxiang:dm:mis:test-user",
+    )
+
+    prompt = RuntimeNoteCaptureAgent.last_ephemeral_prompt or ""
+    assert "Current live runtime for this turn" in prompt
+    assert "model=claude-sonnet-4-6" in prompt
+    assert "provider=gac_anthropic" in prompt
+
+
 @pytest.mark.asyncio
 async def test_run_agent_interim_commentary_works_with_tool_progress_off(monkeypatch, tmp_path):
     adapter, result = await _run_with_agent(
diff --git a/tests/gateway/test_stream_consumer.py b/tests/gateway/test_stream_consumer.py
index 38532e66..2e3c9f65 100644
--- a/tests/gateway/test_stream_consumer.py
+++ b/tests/gateway/test_stream_consumer.py
@@ -6,6 +6,7 @@ from unittest.mock import AsyncMock, MagicMock
 
 import pytest
 
+from gateway.platforms.base import _strip_trailing_display_blocks
 from gateway.stream_consumer import GatewayStreamConsumer, StreamConsumerConfig
 
 
@@ -963,3 +964,13 @@ class TestFilterAndAccumulateIntegration:
             await task
         except asyncio.CancelledError:
             pass
+
+
+class TestDisplayCleanup:
+    def test_strip_trailing_display_blocks_removes_block_cursor(self):
+        text = "Hello world █"
+        assert _strip_trailing_display_blocks(text) == "Hello world"
+
+    def test_strip_trailing_display_blocks_keeps_normal_content(self):
+        text = "Hello world"
+        assert _strip_trailing_display_blocks(text) == "Hello world"
diff --git a/tools/send_message_tool.py b/tools/send_message_tool.py
index 391e03ba..32bd79e7 100644
--- a/tools/send_message_tool.py
+++ b/tools/send_message_tool.py
@@ -19,6 +19,7 @@ logger = logging.getLogger(__name__)
 _TELEGRAM_TOPIC_TARGET_RE = re.compile(r"^\s*(-?\d+)(?::(\d+))?\s*$")
 _FEISHU_TARGET_RE = re.compile(r"^\s*((?:oc|ou|on|chat|open)_[-A-Za-z0-9]+)(?::([-A-Za-z0-9_]+))?\s*$")
 _WEIXIN_TARGET_RE = re.compile(r"^\s*((?:wxid|gh|v\d+|wm|wb)_[A-Za-z0-9_-]+|[A-Za-z0-9._-]+@chatroom|filehelper)\s*$")
+_DAXIANG_TARGET_RE = re.compile(r"^\s*((?:mis|group):[-A-Za-z0-9_@.]+|[-A-Za-z0-9_@.]+)\s*$")
 # Discord snowflake IDs are numeric, same regex pattern as Telegram topic targets.
 _NUMERIC_TOPIC_RE = _TELEGRAM_TOPIC_TARGET_RE
 _IMAGE_EXTS = {".jpg", ".jpeg", ".png", ".webp", ".gif"}
@@ -68,7 +69,7 @@ SEND_MESSAGE_SCHEMA = {
             },
             "target": {
                 "type": "string",
-                "description": "Delivery target. Format: 'platform' (uses home channel), 'platform:#channel-name', 'platform:chat_id', or 'platform:chat_id:thread_id' for Telegram topics and Discord threads. Examples: 'telegram', 'telegram:-1001234567890:17585', 'discord:999888777:555444333', 'discord:#bot-home', 'slack:#engineering', 'signal:+155****4567'"
+                "description": "Delivery target. Format: 'platform' (uses home channel), 'platform:#channel-name', 'platform:chat_id', or 'platform:chat_id:thread_id' for Telegram topics and Discord threads. Examples: 'telegram', 'telegram:-1001234567890:17585', 'discord:999888777:555444333', 'discord:#bot-home', 'slack:#engineering', 'signal:+155****4567', 'daxiang:mis:herenqing'"
             },
             "message": {
                 "type": "string",
@@ -151,6 +152,7 @@ def _handle_send(args):
         "slack": Platform.SLACK,
         "whatsapp": Platform.WHATSAPP,
         "signal": Platform.SIGNAL,
+        "daxiang": Platform.DAXIANG,
         "bluebubbles": Platform.BLUEBUBBLES,
         "qqbot": Platform.QQBOT,
         "matrix": Platform.MATRIX,
@@ -246,6 +248,10 @@ def _parse_target_ref(platform_name: str, target_ref: str):
         match = _WEIXIN_TARGET_RE.fullmatch(target_ref)
         if match:
             return match.group(1), None, True
+    if platform_name == "daxiang":
+        match = _DAXIANG_TARGET_RE.fullmatch(target_ref)
+        if match:
+            return match.group(1), None, True
     if target_ref.lstrip("-").isdigit():
         return target_ref, None, True
     return None, None, False
@@ -409,6 +415,8 @@ async def _send_to_platform(platform, pconfig, chat_id, message, thread_id=None,
             result = await _send_whatsapp(pconfig.extra, chat_id, chunk)
         elif platform == Platform.SIGNAL:
             result = await _send_signal(pconfig.extra, chat_id, chunk)
+        elif platform == Platform.DAXIANG:
+            result = await _send_daxiang(pconfig, chat_id, chunk)
         elif platform == Platform.EMAIL:
             result = await _send_email(pconfig.extra, chat_id, chunk)
         elif platform == Platform.SMS:
@@ -1093,6 +1101,26 @@ async def _send_qqbot(pconfig, chat_id, message):
         return _error(f"QQBot send failed: {e}")
 
 
+async def _send_daxiang(pconfig, chat_id, message):
+    try:
+        import httpx
+        from gateway.platforms.daxiang import DaxiangAdapter
+
+        adapter = DaxiangAdapter(pconfig)
+        if not adapter._access_token:
+            adapter._access_token = adapter._load_access_token()
+        if not adapter._access_token:
+            return _error("DaXiang token missing. Configure DAXIANG_ACCESS_TOKEN or DAXIANG_TOKEN_STORAGE_PATH")
+        if not adapter._user_mis:
+            adapter._user_mis = adapter._extract_user_mis(adapter._access_token)
+        async with httpx.AsyncClient(timeout=30.0) as client:
+            adapter._client = client
+            await adapter._send_text(chat_id, message)
+        return {"success": True, "platform": "daxiang", "chat_id": chat_id}
+    except Exception as e:
+        return _error(f"DaXiang send failed: {e}")
+
+
 # --- Registry ---
 from tools.registry import registry, tool_error
 
diff --git a/toolsets.py b/toolsets.py
index 2e7a0a92..15717d56 100644
--- a/toolsets.py
+++ b/toolsets.py
@@ -359,6 +359,12 @@ TOOLSETS = {
         "includes": []
     },
 
+    "hermes-daxiang": {
+        "description": "DaXiang bot toolset - enterprise messaging via 美团大象 (full access)",
+        "tools": _HERMES_CORE_TOOLS,
+        "includes": []
+    },
+
     "hermes-qqbot": {
         "description": "QQBot toolset - QQ messaging via Official Bot API v2 (full access)",
         "tools": _HERMES_CORE_TOOLS,
@@ -392,7 +398,7 @@ TOOLSETS = {
     "hermes-gateway": {
         "description": "Gateway toolset - union of all messaging platform tools",
         "tools": [],
-        "includes": ["hermes-telegram", "hermes-discord", "hermes-whatsapp", "hermes-slack", "hermes-signal", "hermes-bluebubbles", "hermes-homeassistant", "hermes-email", "hermes-sms", "hermes-mattermost", "hermes-matrix", "hermes-dingtalk", "hermes-feishu", "hermes-wecom", "hermes-wecom-callback", "hermes-weixin", "hermes-qqbot", "hermes-webhook"]
+        "includes": ["hermes-telegram", "hermes-discord", "hermes-whatsapp", "hermes-slack", "hermes-signal", "hermes-bluebubbles", "hermes-homeassistant", "hermes-email", "hermes-sms", "hermes-mattermost", "hermes-matrix", "hermes-dingtalk", "hermes-feishu", "hermes-wecom", "hermes-wecom-callback", "hermes-weixin", "hermes-daxiang", "hermes-qqbot", "hermes-webhook"]
     }
 }
 
